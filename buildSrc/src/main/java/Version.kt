object PluginVersion {
    const val KOTLIN_VERSION = "1.6.0"
    const val GRADLE_VERSION = "7.0.0"
    const val NAV_SAFE_ARGS_VERSION = "2.4.0"
}

object AndroidVersion {
    const val MIN_SDK_VERSION = 21
    const val COMPILE_SDK_VERSION = 31
    const val TARGET_SDK_VERSION = COMPILE_SDK_VERSION
    const val BUILD_TOOLS_VERSION = "30.0.3"
    const val VERSION_CODE = 1
    const val VERSION_NAME = "1.0.0"

    const val APPLICATION_ID = "com.pointo.moviemanager"
}

object Version {

    const val CORE_KTX_VERSION = "1.7.0"
    const val FRAGMENT_KTX_VERSION = "1.4.1"

    // Appcompat
    const val APPCOMPAT_VERSION = "1.4.1"

    // Material
    const val MATERIAL_VERSION = "1.5.0"

    // ConstraintLayout
    const val CONSTRAINT_LAYOUT_VERSION = "2.1.3"

    // RecyclerView
    const val RECYCLER_VIEW_VERSION = "1.2.1"

    // Architecture MVVM & LiveData
    const val LIFECYCLE_VERSION = "2.3.1"

    // Navigation Components
    const val NAVIGATION_VERSION = "2.4.0"

    // Retrofit
    const val RETROFIT_VERSION = "2.9.0"

    // Dagger
    const val DAGGER_HILT_VERSION = "2.39.1"
    const val DAGGER_HILT_ANDRIODX = "1.0.0"
    const val DAGGER_HILT_ANDRIODX_LIFECYCLE = "1.0.0-alpha03"

    // Coroutines
    const val COROUTINES_VERSION = "1.6.0"

    // Chucker
    const val CHUCKER_VERSION = "3.5.2"

    //Moshi
    const val MOSHI_VERSION = "1.13.0"

    //Bindables - DataBinding
    const val BINDABLES_VERSION = "1.0.4"

    //Image Loading
    const val COIL_VERSION = "1.4.0"

    //Viewpager Indicator
    const val VIEWPAGER_INDICATOR_VERSION = "1.2.1"

    //Swipe Refresh Layout
    const val SWIPE_REFRESH_LAYOUT_VERSION = "1.1.0"

    //Datastore
    const val DATASTORE_VERSION = "1.0.0"
}

object TestVersion {

    // AndroidX 
    const val androidXTestCoreVersion = "1.3.0"
    const val androidXTestExtKotlinRunnerVersion = "1.1.2"
    const val archTestingVersion = "2.1.0"

    // Junit5
    const val junitVersion = "4.13.2"

    const val coroutinesTestVersion = "1.6.0"

    // MockWebServer
    const val mockWebServerVersion = "4.8.1"

    // MockK
    const val mockKVersion = "1.10.6"

    // Mockito
    const val mockitoVersion = "2.2.0"

    // Mockito Inline
    const val mockitoInlineVersion = "3.8.0"

    // Mockito Inline
    const val turbineVersion = "0.7.0"
}